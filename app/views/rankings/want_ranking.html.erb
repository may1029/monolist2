<table class="type05">
  <tr>
    <th scope="row">人気アイテム名</th>
    <td>Want数</td>
  </tr>
  <% @want_ranking.each do |item_id, count| %>
    <tr>
        <th scope="row"><%= Item.find_by(id: item_id).title %></th>
        <td><%= count %> </td>
    </tr>
  <% end %>
</table>

